загружаем все пулы из базы при старте приложения

контролируем наличие ресурсов при первом использовании пула




Замечания Виталия:

Всё лежит в этой директории "vagrant/prod"
Обрати внимание на пути в Pipfile ("./../../backend")

Secret_key ещё в настройках есть, для продакшна изменить нужно

Local_settings нужно переделать. Его не из пакета vdi импортировать, а из корневой папки (backend).
Потому что в проде пакет vdi будет лежать бог знает где. Я уверен, Саша справится
